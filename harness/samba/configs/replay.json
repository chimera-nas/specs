{
  "family": "smb2",
  "description": "the exactly-once layer: ChannelSequence and SMB2_FLAGS_REPLAY_OPERATION stamped on ordinary requests. Identical server capabilities to durable.json, and deliberately so: a replay is only answerable from a CreateGuid a server that grants durability bothered to record. Generated but NOT driven -- run_samba_mbt.sh configures smbd with `durable handles = no`, and smb2_replay.py skips any trace whose profile turns a caching capability on, so this cell reports a ctest SKIP.",
  "traceStem": "smb2Replay",

  "features": {
    "oplocks": "supported",
    "leases": "supported",
    "dirLeases": "unsupported",
    "durable": "supported"
  },

  "policies": {
    // 0x0300
    "dialect": 768,
    "leaseOpenBreak": true,
    "forceLevel2": false
  },

  // No deviations: nothing in this corpus is ever replayed against Samba, so
  // enabling one here would be a claim nothing checks.
  "deviations": [],

  "batches": [
    { "flavor": "stepReplay",  "steps": 300, "traces": 8, "seed": "0x51" }
  ]
}
